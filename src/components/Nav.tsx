import React from 'react';
import { Navbar } from 'react-bootstrap';

const Nav = () => (
  <header>
    <Navbar fixed="top" bg="light" expand="lg">
      <Navbar.Brand href="#home">wefox Posts</Navbar.Brand>
    </Navbar>
  </header>
);

export default Nav;
